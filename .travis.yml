language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t arasch13/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true arasch13/docker-react npm run test